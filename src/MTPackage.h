/* Header notes..
    Combines the two MemoryTracker and GlobalOverloads into one header to then include
    in the cpp the user wishes to include in.
*/

#ifndef MT_PACKAGE_H
#define MT_PACKAGE_H

#include "../include/MemoryTracker.h"
#include "../include/GlobalOverloads.h"

#endif